/*
// HiveDialog.cpp
#if defined(WIN32) || defined(_WIN64)
#include <windows.h>
#endif
#include "HiveDialog.h"

using namespace std;

// DataSourceName;

// Description

// Host

// Port

// Database

// Username

// Password

HiveDialog::HiveDialog() {
	HWND htextbox = CreateWindowW(L"static", L"", WS_BORDER | ES_MULTILINE | WS_CHILD | WS_VISIBLE, 50, 0,
		100, 50, NULL, NULL, NULL, NULL);
}

HiveDialog::~HiveDialog(){

}*/